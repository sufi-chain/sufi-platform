@* Page title is managed by SufiPageTitle *@
<SufiPageTitle />

@* Render toolbar items from the Toolbar parameter (PageToolbar contributor system) *@
@if (ToolbarItemRenders.Any())
{
    @foreach (var render in ToolbarItemRenders)
    {
        @render
    }
}

# Background Jobs Architecture

The module follows the standard admin-module layout with contracts, application, API, client, Blazor UI, and shared domain metadata.

The module appears focused on administration rather than business-domain modeling.

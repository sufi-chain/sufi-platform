# Permission Management API

The module provides `HttpApi` and `HttpApi.Client` layers for permission-management functionality.

When expanding this page, document grant retrieval and update flows, provider naming, and integration patterns.

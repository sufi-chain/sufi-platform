# Permission Management Features

Use this page to map permission-grant scenarios to the reusable contracts and APIs that support authorization workflows across the platform.

## Core capabilities

- wrap ABP permission-management application services and contracts
- provide a platform-level package for permission integration
- support other modules, especially identity administration

## Cross-module role

This module is foundational for admin workflows even when it does not expose its own large standalone UI.

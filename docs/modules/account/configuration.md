# Account Configuration

This module primarily depends on the host application's authentication and account configuration.

When expanding this page, document:

- login and registration behavior in the host
- public versus authenticated routes
- any host-specific remote service configuration

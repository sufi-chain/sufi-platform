# Account API

The Account module provides a focused HTTP API surface for account-oriented user flows.

## Controllers in source

The HTTP API layer includes:

- `AccountController`
- `ProfileController`
- `DynamicClaimsController`

## Notes

This module is intentionally smaller than Identity. It focuses on account-facing workflows, while broader user and role administration belongs to the Identity module.

# Tenant Management Configuration

When expanding this page, document:

- host versus tenant administration behavior
- tenant resolution strategy in the host
- feature and settings integration per tenant
- any edition or plan-like conventions if adopted later

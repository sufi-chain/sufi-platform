# Tenant Management API

The module provides `HttpApi` and `HttpApi.Client` layers over tenant-management functionality.

When expanding this page, document tenant CRUD operations, tenant store access, and any remote-service conventions needed by tiered Blazor hosts.

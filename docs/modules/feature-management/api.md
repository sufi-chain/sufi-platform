# Feature Management API

The module provides `HttpApi` and `HttpApi.Client` layers over feature-management functionality.

## Controllers in source

The HTTP API layer includes:

- `FeaturesController`

## Notes

The module wraps ABP feature-management behavior in Sufi Platform packages so it can participate cleanly in platform documentation, packaging, and module integration.

# Feature Management Architecture

This module primarily wraps ABP feature management packages and provides a Sufi documentation and packaging surface around them.

Its Blazor project exists even though there are no obvious standalone `Pages` files in the current source snapshot, which suggests the UI may be embedded through components or reused inside other management experiences.
